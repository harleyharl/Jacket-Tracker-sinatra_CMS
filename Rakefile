require_relative './config/environment' #loads our environment

#this line gives us all of our rake tasks (rake db:migrate etc etc)
require 'sinatra/activerecord/rake'

task :console do
  Pry.start
end
